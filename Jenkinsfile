pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'build - this is hello world program'
            }
        }

        stage('test') {
            steps {
                echo 'test - this is hello world - Testing webhook'
            }
        }
    }
}
